# Interior Akustik — Preview

Ini adalah preview statis yang dibuat otomatis berdasarkan kuesioner Anda. Untuk menjalankan lokal:

1. npm install
2. npm run dev

Atau build: npm run build && npm run preview

Catatan: ini adalah scaffold statis untuk preview; integrasi backend untuk form (/api/lead) dan foto portfolio belum termasuk.
